extends base

block content 
    div
        h4=video.title
        p=video.description
        p #{video.meta.views} / #{video.meta.rating}
        small=video.createdAt
    a(href=`/videos/${video.id}/edit`) Edit Video
    br
    a(href=`/videos/${video.id}/delete`) Delete Video
    
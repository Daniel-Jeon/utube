extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="아이디(이메일)", type="email", name="email", required)
        input(placeholder="PW", type="password", name="password", required)
        input(type="submit", value="LOGIN")
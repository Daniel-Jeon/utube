extends ../base
include ../mixins/video

block content
    each video in user.videos
        +video(video)
    else
        li 업로드한 영상이 없습니다.

    if (loggedInUser._id)
        a(href=`/users/edit`) 
            span Edit Profile >